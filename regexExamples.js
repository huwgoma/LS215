// /a/       //=> Matches 'a'

// /abc/     //=> Matches 'abc'

// /(ab|bc)/ //=> Matches 'ab' or 'bc'

// /[abc]/   //=> Matches 'a', 'b', or 'c'

// /\w/      //=> Matches any word character
// /\d/      //=> Matches any digit character
// /\s/      //=> Matches any whitespace character
// /\b/      //=> Matches any word boundary character

// /a*/      //=> Matches 'a' 0 or more times
// /a+/      //=> Matches 'a' 1 or more times
// /a?/      //=> Matches 'a' 0 or 1 times
// /a{3}/    //=> Matches 'a' exactly 3 times
// /a{5,}/   //=> Matches 'a' 3 or more times
// /a{3, 5}/ //=> Matches 'a' between 3-5 times


/^a/      //=> Matches 'a' at the start of each line
/a$/      //=> Matches 'a' at the end of each line

a



